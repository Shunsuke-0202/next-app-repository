name: Deploy App Service (Prod)

# 本番環境デプロイ用ワークフロー。
# 本番運用では承認フローや安全チェックを追加する想定。
on:
  # GitHub の UI から手動実行する
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure environment validation
        run: |
          # 本番用の前提条件チェックを実行する
          bash .github/scripts/azure-check.sh

      - name: Placeholder deployment
        run: |
          # 本番デプロイコマンドを追加する場所
          echo "Deploy to App Service (prod)"
          echo "Add Azure login and deployment commands here."
